import React from 'react'
import '../../global.d.ts';

const Main = ({strOpenUrl}) => {
    if(strOpenUrl === '/main')
    return(
        <>
            main
        </>
    )
}

export default Main;